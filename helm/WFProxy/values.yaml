appname: wf-proxy
namespace: wavefront
replicaCount: 3
storageClass: aws-efs

wavefront:
  url: https://facke/api/
  token: facke

image:
  name: wavefronthq/proxy
  version: latest

proxy:
  javaargs: ""

config:
  ports:
    - option: pushListenerPorts
      values:
        - 2878
